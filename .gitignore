node_modules
dist
.DS_Store
*.local

# Environment variables (Supabase keys)
# Внимание: маска *.local выше покрывает .env.local, но НЕ .env —
# до 2026-08-14 этого блока здесь не было.
.env
.env.*
!.env.example

# Claude Code local tool-permission settings (per-machine, not project config)
.claude/settings.local.json
