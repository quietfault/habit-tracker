node_modules
dist
.DS_Store
*.local

# Environment variables
# Проект .env не использует: ключи Supabase — константы в src/supabaseClient.js
# (publishable-ключ, публичен по дизайну). Блок оставлен на случай, если .env
# когда-нибудь появится: маска *.local выше покрывает .env.local, но НЕ .env.
.env
.env.*

# Claude Code local tool-permission settings (per-machine, not project config)
.claude/settings.local.json
